{% if fxtd_0.rows %}
<table>
  <thead>
    <tr>
      <th scope="col" style="text-align: left">Type</th>
      <th scope="col" style="text-align: right">Count</th>
      <th scope="col" style="text-align: right">Per kLOC</th>
      <th scope="col" style="text-align: center">Grade</th>
    </tr>
  </thead>
  <tbody>
    {% for row in fxtd_0.rows %}
    <tr>
      <td style="text-align: left">{{ row.type }}</td>
      <td style="text-align: right">{{ "{:,d}".format(row.count) }}</td>
      {% if row.metric is defined %}<!-- FIXME: This is still quite "yarchy"! -->
      <td style="text-align: right">{{ "{:.2f}".format(row.metric.score) }}</td>
      <td style="text-align: center; color: var(--pico-muted-color); background-color: {{ row.metric.color }}">{{ row.metric.grade }}</td>
      {% endif %}
    </tr>
    {% endfor %}
  </tbody>
  <tfoot>
    <tr>
      <td style="text-align: left">Composite (weighted)</td>
      <td style="text-align: right">{{ "{:,d}".format(fxtd_0.grand_total) }}</td>
      {% if fxtd_0.metric_composite_weighted.score is defined %}
      <td style="text-align: right">{{ "{:.2f}".format(fxtd_0.metric_composite_weighted.score) }}</td>
      <td style="text-align: center; color: var(--pico-muted-color); background-color: {{ fxtd_0.metric_composite_weighted.color }}">{{ fxtd_0.metric_composite_weighted.grade }}</td>
      {% endif %}
    </tr>
  </tfoot>

</table>

{% else %}

<h6>Congratulations..NO Fixme/ToDo... issues!</h6>

{% endif %}
